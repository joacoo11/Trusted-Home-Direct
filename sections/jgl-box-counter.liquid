<div class="jgl-counter">
    <div class="step-box counter">123</div>
    <div class="step-box counter">1234</div>
    <div class="step-box counter">12345</div>
    <div class="step-box counter">123456</div>
    <div class="step-box counter">1234567 </div>
</div>

<style>

</style>

<script>
  window.addEventListener("scroll", function(){
    if (isInViewport(document.getElementsByClassName("counter")) ) {
      let i = 0;
      let counter = setInterval(function(){
        document.getElementsByClassName("counter").innerHTML = i;
        i++;
        if (i > 597432) {
          clearInterval(counter);
        }
      }, 1);
    }
  });

  function isInViewport(element) {
    var rect = element.getBoundingClientRect();
    var html = document.documentElement;
    return (
      rect.top >= 0 &&
      rect.left >= 0 &&
      rect.bottom <= (window.innerHeight || html.clientHeight) &&
      rect.right <= (window.innerWidth || html.clientWidth)
    );
  }
</script>

{% schema %}
    {
        "name": "JGL Box Counter",
        "settings": [

        ],
        "presets": [
    {
        "name": "JGL Box Counter"
    }]
}
{% endschema %}